# Changelog

## 0.3.x

- **0.4.0 — 2026-09-12: the PC on the watch's bus.**
  - Bot, `bot.quiet_when_watch` (default on; Franz via the master, 12/09 15:26: with the app on the wrist a question arrived twice, the app's own notification plus Wear OS forwarding the Telegram one): when the relay has a paired watch (`devices.json`) and its last push succeeded less than `bot.watch_fresh_s` (180 s) ago, the Telegram notices for questions, outcomes, answers, errors and vanished sessions go out with `disable_notification` (they stay in the chat for the phone, they do not vibrate or reach the wrist); a failed push or no paired watch and they sound again; the recap asked on purpose always sounds.
  - Bot, the session link opens in the right place for its account (Franz via the master, 12/09 12:23: the official Claude app keeps one login, the other account lives in Chrome): `bot.links = {account: app|browser}`; the card and the question notice carry a URL button — «Apri sessione» (https, the app takes it) for `app`, «Apri in Chrome» as a Chrome intent (`intent://…#Intent;scheme=https;package=com.android.chrome;end`) for `browser`; when Telegram refuses the intent button (BUTTON_URL_INVALID) the same message is resent with the link as an «Apri in Chrome» anchor in the text and the other buttons kept. `bot.links` is a free-form map in the config merge.
  - `claude-master relay` (phase 1 of the Wear OS app, design `docs/plans/2026-09-12-app-polso-design.md`): the PC on the watch's bus. `relay push` builds `/state` from the existing sources (`sessions --json`, the registry snapshot for gone sessions, the ledger, `answer --show` for open questions, `quota --json`, the recap cache, the night queue, the project inventory, the bot's follow/awaiting state, the transcript tail for the tool in use) in the v1 contract shape (`tests/fixtures/relay/state-*.json`, identical to `claude-master-watch/contract/`), encrypts it AES-256-GCM (`{"v":1,"enc":…}`), writes it to Firebase RTDB, appends `/events` from the diff with the previous push (question, answered, outcome, gone, launched, quota) and sends one FCM data message per event; `--dry-run` prints the clear JSON without the network, `--async` returns at once and pushes after a 2 s debounce (many hooks = one push); over 8 KB the recap items, the outcomes' `full` and the projects past ten are dropped, never the question. `relay pair`: a six-digit code on screen, X25519 + HKDF over `/pair/<code>`, the key in `~/.claude-master/relay/key` (0600), one uid per device in `/allowed` (a new pairing revokes the old one). `relay serve`: SSE on `/cmd`, allow-listed execution through the CLI (answer with a git checkpoint, prompt with the Watch: line requested, launch only on published projects, follow/unfollow, resume, screen with 30 lines, allow_all when the question has a «don't ask again» option and is not high-tier), encrypted `/result`, republish, duplicates dropped, backoff 1-2-5-15-30 s, lock and pidfile; `relay ensure|status|install|uninstall|off` (cron: ensure + a heartbeat push every minute). The hooks (SessionStart, SessionEnd, PermissionRequest, Stop) and the bot's follow/unfollow call `push --async`; `waiting/<sid>` is now JSON `{tool, input}` (the tier: rm -rf, git push, deploy, DROP, ssh, sudo, --force → high; reading tools → low); `UserPromptSubmit` logs a `prompt` ledger row (the turn start). No Firebase SDK: REST + SSE with urllib, the service account's OAuth2 token from a signed JWT (`cryptography`, a soft dependency). Config `relay.*` (off by default). Tests against a fake RTDB/FCM/token server (`tests/lib/cm_test.fake_rtdb`).
- **0.3.10 — 2026-09-12: full width, and only what the buttons cannot say.**
  - Bot, the text says only what the buttons cannot (Franz 12/09 11:12: the options were shown in the text and repeated in the buttons): the question notice lists the options only when they carry a description («1 Firebase RTDB + FCM · Zero infrastruttura, sveglia push vera…», one whole line each, from the dialog on screen or from the hook payload), when they are more than three (the buttons are two + Apri, the others are dictated by number) or when there can be no buttons (no tmux); with up to three plain options the message is the question alone and the buttons choose. The card lists the options only past three.
  - Bot, full width (Franz 12/09 11:14 and 11:18, screenshots of the phone and the Pixel Watch: the Telegram bubble is as wide as its longest line, and 22-character lines left it at half the screen with cut text): one logical line = one physical line, no forced wrapping and no «…» in message bodies; short adjacent lines of the same kind are joined with « · » so the first line is the widest («▶ 🔴 master · lavora 2m», «❓ 🔴 master · Trasporto», «▶ 🔴 master · 2m · Bash pytest -q tests», «📤 🔴 master · «your words»», «📍 🔴 api · 5h · settimana», «✗ 🔴 master errore · rate limit reached», «⚠ 🔴 master · non ha ricevuto»); the question, the options, the outcome and the «→ next» whole (the ~1200-character cut with «Leggi tutto» stays); a busy card no longer repeats the duration in an «in corso da» line and hides the stale outcome; only button labels are shortened. The tests assert zero «…» in bodies and a line ≥ 22 characters wherever the content allows.
  - Bot, the synthesized question is expandable (via the master, 12/09 11:38): when the notice's synthesis dropped part of the question, a «Domanda intera» button (`q:name`) sends the whole text, as «Leggi tutto» does for an outcome; the card offers it too while the whole question is on file; no button when the question was shown whole.
  - `answer` parse: «Type something.» and «Chat about this» (and anything after them) are the dialog's footers, not options (via the master, 12/09, from Franz's screenshot); the fake claude renders them like the real dialog.
- **0.3.9 — 2026-09-12: the question reaches the watch whole.**
  - Bot, the question reaches the watch WHOLE (Franz 12/09 10:54: the master's question arrived cut and could not be answered): the screen parser joins the question's lines (the second line was lost), the hook's payload question (never line-broken) wins over the screen, and the notice shows the question's gist — the whole text when it fits 88 characters, else the last interrogative sentence that fits (the context sentence dropped), else a one-sentence synthesis by the model (`hooks.ask_notify.synth_model`, haiku, `claude -p`; empty to disable), else the text wrapped over 4 lines — over as many lines as it takes, then the options one per line; «(Recommended)» dropped from the labels. The card shows the same gist over up to 3 lines. The «options in short» line of the three-line notice is gone: it could not hold a real question.
- **0.3.8 — 2026-09-12: the watch sees what the desktop sees.**
  - Bot, parity with the desktop for a prompt from the watch (12/09): ONE live message «📤 name» + the echo of what you dictated, edited in place from the session's transcript (`projects/<slug>/<sessionId>.jsonl`) every `bot.live_edit_s` (3 s) while it works — «▶ name al lavoro», the tool in use («Bash pytest -q tests»), the last two lines it wrote, «▶ name · 2m», «❓ name aspetta te» — with Ferma (Esc in the tmux pane, «⏹ name interrotta»), Terminale (the last screen lines) and Sessioni under it; «⚠ name non ha ricevuto» + Invia di nuovo (the same prompt with `--force`) when the prompt does not show up in the transcript within `bot.receive_timeout_s` (15 s); one silent «⏳ name ancora al lavoro» past `bot.answer_timeout_s` (30 min); «✗ name errore» + the first error line on a StopFailure. Answering a question from the watch starts the same live message. `bot serve` shortens its long poll to the edit cadence while a live message exists.
  - Bot, the «Watch:» line: the prompt prefix says «Da Franz via Telegram (watch)…» and asks the session to end with `Watch: <the bare outcome, ≤ 60 characters>`; the Stop hook stores it in the ledger (`watch`); the answer is «✓ name» + that line alone, «Leggi tutto» for the rest. The answer to a wrist prompt is the Stop that carries the Watch line — or the first Stop if the session was idle when the prompt left, or the second if it was busy (the first is the previous turn, shown as «ha finito» only under the follow rule): before, a busy session's previous turn was announced as the answer.
  - Bot, button names (12/09 09:26, rule: verbs for actions, nouns for destinations): Elenco → Sessioni; Indietro → «◀ name» (the card you came from, under quota, help, Leggi tutto, confirmations and errors); Schermo → Terminale; Segui / Smetti di seguire → Avvisami / Basta avvisi; Riprendi → Continua, shown only on ✓ idle and ✗ gone sessions; Annulla → Annulla modifiche, shown in the card only when a checkpoint exists (it was in the after-send keyboard and read as «cancel the prompt»); Tutto → Leggi tutto; Interrompi → Ferma; Riprova → Invia di nuovo. New dictable words `avvisami`, `continua`, `ferma`/`stop`, `terminale`, `riprova`; the old ones stay as aliases. The digest carries an «Apri name» button per pending question (max 4).
  - Bot, from the watch census (claude-watch, Handwave, ccwearos; via the master, 12/09): the question notice is decided by its first three lines (Wear OS shows only those large) — «❓ icon name», the question cut to one line, the options in short «1 rosso · 2 blu · …» — then the options one per line for the phone, and at most three buttons (the options when they are ≤ 3, otherwise the first two + «Apri name», the card has them all); `Terminale` / `schermo NAME` / `v NAME` show the last 30 lines of the session's pane in a `<pre>` block (HTML) instead of 8 cut lines; the «Esito:» line of outcomes and answers is ONE sentence of ≤ 120 characters for voice reading (list markers stripped, a path shortened to its last piece), what followed the first sentence goes to the next line; the card opens with the state icon («▶ 🔴 master», then «lavora 3m»), so every session message starts with ❓ ▶ ✓ ✗ (or 📤 📍 ⚠ ⏹ ⏳ for the live flow).
  - Bot, state you can see: «📍 name» heads quota and help when you are in a card; the card does not expire while its session's answer is awaited (a text after 10 minutes went «prima scegli la sessione»); at list level a text goes to the only session awaiting an answer (or the only followed one) instead of asking.
  - Cron PATH (T81): from cron `PATH=/usr/bin:/bin` and the `claude` link in `~/.local/bin` was not found, so a `/master` tap from the phone answered «AVVIO FALLITO». Every script now prepends `~/.local/bin` and `/usr/local/bin` once (cm-lib), and `launch` falls back to `~/.local/bin/claude`, naming the paths it tried when none works.
  - Bot: `/master`, `/launch` and the button tap no longer launch a session that is already alive on that folder — they answer «già viva» with its status and link.
  - Bot: `bot serve`, a long-polling daemon (`getUpdates` with `timeout=50`: Telegram answers the instant an update arrives, no more waiting for the minute of cron), same offset and dispatch as `poll`, reconnection with backoff (1, 2, 5, 15, 30 s) on network errors, exclusive lock (never two consumers of the token: a second `serve`, or the telegram plugin holding the channel, exits saying why), pidfile and a status file; `bot ensure` restarts it when it is not running (the cron of the minute now calls `ensure`; `restore` calls it too), `poll` stays as a manual fallback and is skipped while `serve` is alive; `bot status` shows pid, uptime, last update, last loop and reconnections. The HTTP timeout of the long call exceeds the poll window (measured live: with the 20 s default every round was «rete assente»); SIGTERM interrupts the wait through an exception.
  - Bot: the answer to a wrist prompt always comes back — after Invia or Riprendi the session is «awaiting» and its first Stop is sent to the wrist whatever the turn's length (normal notification, ≤ 8 clean lines with the «Esito:» line first, a «Tutto» button for the whole text); the delivered prompt is prefixed «Da Franz via Telegram (polso)…» and forbids peer replies (a session had answered the master over SendMessage instead); `bot.follow_min_turn_s` (30) replaces the 2-minute follow threshold.
  - Bot: a free prompt from a session's card — text that is neither a command nor an answer is previewed («A 🟩 shop-acme:» + the text) with Invia / Annulla buttons; Invia takes a git checkpoint of the workspace and sends it through `talk` (native channel when the session is in the peer registry), the session becomes followed so the turn's outcome comes back to the wrist, «inviato a …» silent; at list level free text answers «prima scegli la sessione» with the session buttons; texts under 3 characters are ignored; a session that is not alive is refused.
  - Bot for the wrist (Wear OS, measured live: ~22 characters per line, inline keyboards work, dictation writes «due si»): every message ≤ 8 lines of ≤ 22 characters with the first line decisive; bare dictable words next to `/commands` (`sessioni`/`s`, `master`/`m`, `lancia X`/`l X`, `quota`/`q`, `aiuto`/`?`, `elenco`/`0`, `segui`/`f`, `riprendi`/`t`, `annulla`); a fixed row of buttons Elenco · Quota · Master under everything; the list is one line per session `<state> <short name>` (❓ waiting with two words of the question, ▶ working, ✓ idle with the age past a day, ✗ gone from the last good set), ordered ❓ ▶ ✓ ✗ then alphabetically, with number buttons; a number opens the card (name · account, state, last outcome, «→ next» from the recap, the question with numbered options and their buttons); in a card a number (or «due si») answers through `answer NAME N` as a reply to the card, after a git checkpoint of the workspace (`git stash create`) that `annulla` restores; `f` follows a session (a turn longer than 2 minutes ends → «✓ name: outcome», it disappears → «✗ name»), `t` sends «resume where you were»; chat state expires after 10 minutes; everything silent (`disable_notification`) except questions, followed outcomes and disappearances; `bot digest` at 08:00 («what waits for you»: pending questions, sessions idle since yesterday, ✗), installed by `bot install` together with the Telegram command menu (`setMyCommands`); `/sessions full` keeps the table. The hook notice for a question is compact too, with option buttons (`ans:NAME:N`) and it puts every chat in that session's card.

- **0.3.7 — 2026-09-11: the master leads the odd tilings.**
  - `tile` with an ODD number of windows that include the root session: the master becomes the big window on the left (60 %), the others stacked on the right in N-1 rows (`tile.odd_layout`: `master-primary`, the default, or `uniform` for the old behaviour). Even counts, explicit `--rows`/`--grid`, or a subset without the master keep the uniform layout; a stacked column below `tile.min_column_px` is allowed and reported, never refused.

- **0.3.6 — 2026-09-11: the phone knows when a session stops.**
  - When a session stops on a question or a permission, the `PermissionRequest` hook sends the allowed Telegram chats the question, its numbered options as they appear on the session's screen and «reply “2 to NAME” to the master» (`hooks.ask_notify`, on by default, silent without the `telegram` plugin's token). Text, not an inline keyboard: a tap would reach the plugin's poller, not this one. `claude-master answer` gained the `--notify` entry the hook runs detached.
  - `claude-master screen <name> [--lines N]`: the last lines of a session's terminal, so «screen NAME» to the root session shows from the phone what a session is doing.
  - `recap`: closed projects under `recap.min_turns` (3) turns, or without a sentence, fold into one «other: a, b» line instead of one line each (the per-project `docs/recap.md` still gets its line).
  - `sessions`: NAME is the tmux name, the one every command accepts; `/rename` in the app only changes the peer registry's `name` (verified live), so a renamed session shows as `folder (app-name)` instead of a name `answer`/`talk`/`close` would not find.
  - Registry «last good set» (`registry.good_file`): the reconciled registry only refused to go EMPTY (T19), so four windows closed by hand before the reboot of 11/09 09:51 left one session in it and `restore` relaunched 1 of 5. The snapshot grows with the live set, is replaced only when every session in it is still alive, never shrinks on its own; a session leaves it only through an explicit close (`claude-master close`, `/exit` — `registry --closed NAME`). `restore` proposes the union of registry and snapshot and says where each session comes from, with the date (`registry --good` prints the snapshot).
  - Bot: `/start` answers «launch the root session?» with an inline button; the tap (a `callback_query`, which reaches this poller when no session is alive) launches the master like `/master`.
  - `merge`: the Terminal's start tab (`#home`) cannot be closed while other tabs exist (SWA behaviour, T78); a home-less app window only exists when a session tab was dragged out by hand, so `merge` prefers such a window as the collecting one and, when there is none, says so and uses the one with the start tab.
  - `restart arm`: one flag per session (`<flag_file>-<tmux>.json`); the Stop hook reads only its own. With a single file, eight `arm`s in two minutes (11/09 13:10) gave four restarts: every arm overwrote the previous one in silence. `restart list` (or `restart` alone) shows the armed sessions.
  - «Always tabs» (`terminal.open_as_tab`, on by default, ChromeOS): `launch`, `restart` and `restore` open the session as a TAB of a Terminal window already open (the merge mechanics: a shell tab duplicated, the placeholder consumed by the new shell), preferring a window without the start tab; garcon only when no Terminal window has a shell, and then ONE plain window whose shell becomes the source for the next sessions. `restore` runs one session at a time in that mode (~20 s each) and its dry run says so.
  - `tile` never detaches into popups any more (Franz: «always tabs, even side by side»): a session that shares a window, sits in a maximized/minimized one, or lives in a popup left by earlier versions, is reopened in an app window of its own through garcon (`attach NAME`, a new tmux client awaited, the old tab closed), then tiled.
  - No Terminal window shows the «Terminale» start tab next to a session any more (T80, chrome-bridge `ed24b83`): the start tab cannot be closed while other tabs exist, but it can be pulled out alone into a popup (`move_tab`) and closed there. `launch`/`restart`/`restore`, `tile` and `merge` evict it, by URL only and only when the window has other tabs. Verified live: `launch` of a test session lands as a tab of the working window in 16 s, windows 3→3, start tabs 0, popups 0.

- **0.3.5 — 2026-09-11: README opening names the master session.**

- **0.3.4 — 2026-09-10 (0.3.3 never left the machine).**
  - `doctor` checks the plugin in every account's cache through `claude plugin list --json` (Claude Code 2.1.268): version against the checkout, `errorDetails`/`noteDetails`, disabled or missing, with the fix.
  - Shell wrappers no longer depend on helper functions: Claude Code's shell snapshot captures the per-account wrappers but not the helpers, so `claude --version` inside a session printed «_cm_fn_exists: command not found» (T76).
  - `claude-master answer <name> --show | <n>`: read the question another session is stuck on and answer it by number through its terminal (a message in the inbox does not unblock a dialog); the phone answers through the root session.
  - `claude-master guard`: quota guard from cron — one Telegram warning per window above 95% with the reset time; at the reset, the sessions of that account that hit the wall (a failed turn in the ledger) get «resume where you were» through their inbox, and a pending night queue is run.
  - `claude-master recap`: the next step per project (same model call), under the open projects and in `docs/recap.md`.
  - Kernel rule 9: every turn ends with one line of outcome, never a bare «Fatto.» (feeds the recap, `next`, the registry).
  - A session starts knowing the last days: SessionStart prints the last `recap.startup_lines` (5) lines of the project's `docs/recap.md` after the kernel.
  - `diary` is now `recap` (`claude-master recap`, `/recap`; the old names stay as aliases, a `diary` block in config.json is read as `recap`): per project, one sentence on the whole day written by a cheap model from every end-of-turn message (cached per day), the real tab icons, the link on the name, no times or turns; the same line is appended to each project's `docs/recap.md` (`recap.project_log`, one line per date, rewritten if the day goes on).
  - `recap`: per project instead of per session (restarts merged, «restarted N×»), three groups in the order you need them (waiting on a question, alive, closed today; closed with zero turns hidden), a link to open every live project, the last message quoted only for the waiting ones and never a line with an email or a letter heading (`diary.last`: none | short | full); Telegram in HTML; `--full` keeps the old per-session list; `/recap` from the bot.

- **0.3.2 — 2026-09-10: the README, for people who have never seen it.**
  - README rewritten for readers who have never seen the plugin: who it is for, what you get with four illustrated cards (`assets/readme/`, generated by `tools-readme-cards.py`), what you are trusting, why not plain tmux, a minimal configuration; the command reference moved to the end. Reviewed by two other model families before landing.

- **0.3.1 — 2026-09-10: the first night with the legacy scripts gone.**
  - `launch` waits up to `session.link_wait_s` (10 s) for the Remote Control link: the peer registry file exists before the bridge is connected, and `--continue` reported «not yet in the registry» while the link was seconds away.
  - `restore` without a terminal stops at the list and asks for `--yes` (it used to relaunch every missing session when read from a Claude session).
  - `cloud` without a TTY runs `claude --cloud` in a detached tmux session with `remain-on-exit` and prints link and id (T75).
  - `cutover.sh` reads the crontab before writing it (the `-l | grep | -` pipeline raced on the spool).
  - `claude-master bot`: `/master`, `/launch <fragment>`, `/sessions` from Telegram when no session is alive — a cron poller on the `telegram` plugin's own bot (same token and allowed chats), quiet while a session's plugin is polling, backlog discarded on the first run, one update confirmed at a time, a lock against overlapping runs (phase 9, N2 reduced).
  - `claude-master recap`: the day's diary from the hooks' ledger (per session start→end or alive, turns, waits with tool and time, last assistant message, totals per account), `--send` to the allowed Telegram chats, `recap install` for the 20:00 cron (phase 9, N7).
  - `claude-master night`: a queue of unattended jobs run with `claude -p` one at a time from cron (guards on free RAM and five-hour quota, `--max-turns`, `--permission-mode`, `CLAUDE_CODE_TOOL_MEMORY_LIMIT`, per-item timeout), a report per job in the project's `docs/notte/`, the summary on Telegram (phase 9, N5).
  - `move`: the direction probe no longer lands on the built-in screen when the target monitor has no window (Chrome cannot see it: the message now says to drag one there); monitors measured within 40 px are the same monitor (no more `center1` ghosts); `layout restore`/`list` print a readable summary; `link_wait_s` default 20 s.
  - `desk` is optional and off by default (`desk.enabled`): sessions without a project, a random name and a fixed folder, plus a process always on — the root session stays the phone's single entry point.

- **0.3.0 — unreleased: shell, operator commands, restore, screen layout; first live-verified release (phases 5–7 + live tests).**
  - Shell integration generated by `init --shell`: wrappers per account (`shell.wrappers`), aliases in your language (`shell.aliases`), placeholder consumption for new tabs, post-reboot restore prompt; `init --cron` (registry reconciliation) and `init --tmux` (tile/merge/move keys from `tmux.keybindings`, appended once with `--yes`).
  - `next` (the session that needs you most), `park`/`unpark` (hibernate by name, by idle time or when RAM runs low), `queue` (a prompt for the next Stop), `cloud`/`follow`/`desk`.
  - `restore` after a reboot from the registry; `tile`/`merge`/`move`/`layout` for the ChromeOS Terminal through chrome-bridge, skill `screen-layout`.
  - Live tests on 2026-09-09 fixed four real defects: minimized windows are detached like maximized ones (T72); columns narrower than `tile.min_column_px` fall back to a grid; the duplicated tab is activated so the Terminal starts its shell (T73, merge of 6 sessions in 29 s instead of a timeout); `init` proposes `session.claude_args` from the live sessions, without which a launched session runs in `dontAsk` mode with every Bash denied (T74). chrome-bridge gained `window_layout` in its CLI for `layout`.
  - `release.sh`, README for the public marketplace listing.

## 0.2.x

- **0.2.0 — unreleased: every command of the legacy scripts, hooks, skill (phases 1–4).**
  - `sessions` from the official peer registry of every account (status busy/idle/waiting, tmux pane, Remote Control link, channel), reconciled with /proc; `color`, `attach`, terminal backends (chromeos, gnome, kitty, iterm2, macos-terminal, wt, none), `quota`.
  - `launch` (account from folder map, dialogs answered, startup confirmed by the registry, window verified attached, profiles, `--bg`), `registry`, `close`.
  - `talk` over the session's inbox socket with the reply read from the transcript (tmux typing as fallback), `wait`, `report`, `queue`.
  - Hooks: local time on every prompt, behavioural kernel at session start, registry refreshed on start/end, waiting flag from PermissionRequest, event ledger, armed `restart` executed by the Stop hook (`--clean`, `--switch-account`).
  - Skill `sessions` with the rules that matter more than the scripts; slash commands `sessions`, `quota`, `launch`, `close`, `restart`, `report`; `bin/claude-master` on the Bash tool PATH.

## 0.1.x

- **0.1.0 — unreleased: skeleton (phase 0).**
  - Plugin manifest, development marketplace, one configuration file with machine-wide defaults (`cm-config.py`), `claude-master init` that reads the machine and proposes every value with its source, `claude-master doctor`, message catalog (it/en), test harness with a private tmux server and live-captured fixtures.
