---
description: How full each account's quota is (5-hour and weekly windows, with the age of the reading) — read from disk, no call
allowed-tools: Bash(claude-master:*)
---

Run and report the output AS-IS:

```
claude-master quota ${ARGUMENTS}
```
